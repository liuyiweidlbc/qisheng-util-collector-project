# archive

整理时移入的重复/无关文件，默认不要使用。

| 路径 | 说明 |
|------|------|
| `hosts-extras/` | 与 `tools/fix_hosts.ps1` 功能重叠的旧 hosts 脚本 |
| `prob.json` | 无关球赛数据，误放于此 |
| `adguard_home/` | 空占位，曾计划 AdGuard Home 方案 |

日常请用上层 [`../README.md`](../README.md) 指向的 `client/` / `server/` / `tools/`。
